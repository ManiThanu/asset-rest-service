// We can move to the store architecture like common-services if need be;
// it just seemed like it was not needed or wanted...

import assetFormat  from './assetFormat';
import assetType    from './assetType';
import image        from './image';
import imageMapping from './imageMapping';
import story        from './story';
import storyMapping from './storyMapping';

export default {
  assetFormat,
  assetType,
  image,
  imageMapping,
  story,
  storyMapping,
};
